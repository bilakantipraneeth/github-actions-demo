name: Run Shell Script Workflow

on:
  push:
    branches:
      - main

jobs:
  run-shell-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run shell script
        run: |
          chmod +x ./show_directory.sh
          ./show_directory.sh
