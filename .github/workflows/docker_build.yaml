name: docker build
on: [push]
jobs:
  dockerbuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: docker build
        run: docker build -t shellapp . 